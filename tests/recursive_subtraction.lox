fun recursive_subtraction(x) {
    if (x == 0) {
        print "done!";
    } else {
        print x;
        recursive_subtraction(x-1);
    };
};

// Currently overflows the stack at about 2500 iterations
recursive_subtraction(2000);