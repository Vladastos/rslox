fun recursive_subtraction(x) {
    if (x == 0) {
        print "done!";
    } else {
        print x;
        recursive_subtraction(x-1);
    };
};

// Currently overflows the stack at about 110 iterations!!
// This is not good!
recursive_subtraction(100);
